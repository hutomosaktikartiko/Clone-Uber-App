import 'package:flutter/material.dart';

class EarningsTabPage extends StatelessWidget {
  const EarningsTabPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("This is Earnings Tab Page"),
    );
  }
}